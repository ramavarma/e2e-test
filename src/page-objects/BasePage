import { ConnectOptions, Puppeteer, Browser } from 'puppeteer';

export class BasePage {
    private baseUrl: string = 'http://www.s360.co.nz';
    private browserOptions = {}
    private browser = new Browser();

    constructor(url: string, options?: ConnectOptions) {
        this.baseUrl = url;
        if (!options) {
            options = {
               
            }
        }

    }

    async goTo(): Promise<void> {
        return new Promise<void>((resolve, reject) => {

        });
    }

    getUrl(): string {
        return this.baseUrl;
    }

    getBrowserOptions() {
        return this.browserOptions;
    }


}